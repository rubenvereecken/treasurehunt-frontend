/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["lh3.googleusercontent.com", "ruben30.com", "www.gravatar.com", "placekitten.com", "robohash.org"],
  },
};

module.exports = nextConfig;
